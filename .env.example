# Netlify environment variables — set these in Netlify, never commit real values.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=      # least-privilege published-release reads
BREEZE_OPS_TOKEN=              # functions-only random token for narrow Breeze Ops writes
